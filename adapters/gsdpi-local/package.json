{
  "name": "@bos/adapter-gsdpi-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone Paperclip external adapter candidate for bounded GSD-Pi local execution.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "rm -rf dist && tsc && node --test dist/tests/*.test.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
