{
  "schema_version": "0.1-draft",
  "plugin_key": "bos-light",
  "status_enum": [
    "confirmed",
    "unsupported",
    "fallback-only",
    "unvalidated"
  ],
  "guardrail": "Do not treat draft manifest, worker optional chaining, or in-memory adapter behavior as confirmed Paperclip runtime support. A capability may be marked confirmed only after the named proof command or runtime evidence field is populated from a real Paperclip runtime.",
  "capabilities": [
    {
      "key": "company_template.import_export",
      "status": "unvalidated",
      "paperclip_surface_name": "Company template import/export runtime schema",
      "requirement_ids": ["C4", "A1"],
      "downstream_consumers": ["company-template/bos-company-template.json", "scripts/import-company-template.sh", "company-template/a1-validation-evidence.md"],
      "evidence_source": "Local repository validation only; live Paperclip import/export path has not been exercised.",
      "proof_command": "python3 scripts/validate_company_template.py (local structure only); replace with live Paperclip import/export command evidence before confirming.",
      "runtime_evidence_field": "paperclip.company_template.import_export.schema_version",
      "fallback_path": "Keep semantic company-template assets and AGENTS.md files as human/import-prep artifacts until live import/export compatibility is proven.",
      "blocker_text": "Cannot claim native company import/export support until a real Paperclip export/import run supplies schema/version evidence.",
      "adapter_assumptions": [],
      "notes": "C4 remains unvalidated by design even though A1 local package checks pass."
    },
    {
      "key": "agents.syntax",
      "status": "unvalidated",
      "paperclip_surface_name": "AGENTS.md profile syntax compatibility",
      "requirement_ids": ["C5", "A1"],
      "downstream_consumers": ["agents/Div*_*/AGENTS.md", "company-template/bos-company-template.json"],
      "evidence_source": "Local files exist and reference BOS Light divisions; current Paperclip agent profile parser has not validated the syntax.",
      "proof_command": "python3 scripts/validate_company_template.py (presence/reference check only); replace with Paperclip AGENTS.md syntax validator before confirming.",
      "runtime_evidence_field": "paperclip.agents.syntax.validator_version",
      "fallback_path": "Treat AGENTS.md profiles as markdown operating briefs that can be manually copied or converted if Paperclip syntax rejects them.",
      "blocker_text": "Do not mark template release syntax-compatible until Paperclip accepts the AGENTS.md files.",
      "adapter_assumptions": [],
      "notes": "C5 is separate from C4 because import success and AGENTS parser acceptance can fail independently."
    },
    {
      "key": "plugin.runtime.version_build",
      "status": "unvalidated",
      "paperclip_surface_name": "Plugin runtime version and build metadata",
      "requirement_ids": ["C6"],
      "downstream_consumers": ["plugin-bos-light/src/worker.ts", "future runtime probe", "health report"],
      "evidence_source": "No runtime probe has captured Paperclip version/build in this repository.",
      "proof_command": "Future S02 runtime probe must record version/build from the real Paperclip host before this can be confirmed.",
      "runtime_evidence_field": "paperclip.runtime.version_build",
      "fallback_path": "Fail closed in health reports by showing runtime version/build as unknown and keeping SDK-dependent capabilities unvalidated.",
      "blocker_text": "Cannot set minimum supported runtime or breaking-change posture without live version/build evidence.",
      "adapter_assumptions": ["worker comment: confirm min version and breaking changes after C6"],
      "notes": "This is the root guardrail for all C7 surface claims."
    },
    {
      "key": "plugin.runtime.registration",
      "status": "unvalidated",
      "paperclip_surface_name": "Plugin entrypoint and definePlugin/import-path runtime",
      "requirement_ids": ["C6", "C7"],
      "downstream_consumers": ["plugin-bos-light/src/worker.ts", "manifest.paperclip-plugin.json"],
      "evidence_source": "worker.ts explicitly says the definePlugin import path and SDK wiring are draft pseudo wiring.",
      "proof_command": "Future plugin load smoke test against current Paperclip runtime.",
      "runtime_evidence_field": "paperclip.plugin.entrypoint.loaded",
      "fallback_path": "Keep pure BOS Light logic callable directly while plugin entrypoint support is unknown.",
      "blocker_text": "Do not ship runtime plugin registration until current Paperclip SDK entrypoint is confirmed.",
      "adapter_assumptions": ["confirm definePlugin import path"],
      "notes": "The manifest is a draft contract, not proof that the host can load this worker."
    },
    {
      "key": "registration.tools",
      "status": "unvalidated",
      "paperclip_surface_name": "ctx.tools.register tool registration API",
      "requirement_ids": ["C7", "A2", "A3", "A6", "A7"],
      "downstream_consumers": ["piko:bpi-score", "piko:blueprint-gen", "piko:eval-gate", "piko:decide", "plugin-bos-light/src/worker.ts"],
      "evidence_source": "Draft manifest requests tools.register and worker.ts calls ctx.tools?.register with optional chaining; no host response evidence exists.",
      "proof_command": "Future runtime registration probe invoking registered piko:* tools through Paperclip.",
      "runtime_evidence_field": "paperclip.registration.tools.registered_tool_keys",
      "fallback_path": "Run pure TypeScript functions directly in local tests or expose results through manual issue comments until host tool registration is proven.",
      "blocker_text": "Do not present piko:* tools as host-available until registration and invocation are observed.",
      "manifest_capabilities": ["tools.register"],
      "manifest_tools": ["piko:bpi-score", "piko:blueprint-gen", "piko:eval-gate", "piko:decide"],
      "adapter_assumptions": ["ctx.tools?.register"],
      "notes": "Optional chaining prevents local crashes but can silently skip runtime registration."
    },
    {
      "key": "registration.data",
      "status": "unvalidated",
      "paperclip_surface_name": "ctx.data.register data provider API",
      "requirement_ids": ["C7", "A4"],
      "downstream_consumers": ["betting-table data provider", "dashboard betting table widget", "plugin-bos-light/src/worker.ts"],
      "evidence_source": "Draft manifest requests data.register and worker.ts calls ctx.data?.register; no Paperclip host registration evidence exists.",
      "proof_command": "Future runtime data-provider probe that reads betting-table through Paperclip UI/data APIs.",
      "runtime_evidence_field": "paperclip.registration.data.registered_provider_keys",
      "fallback_path": "Render betting candidates from managed Paperclip issue/project artifacts or generated markdown if data providers are absent.",
      "blocker_text": "Do not rely on dashboard data provider hydration until Paperclip confirms data.register.",
      "manifest_capabilities": ["data.register"],
      "adapter_assumptions": ["ctx.data?.register"],
      "notes": "The current worker returns an empty data provider placeholder."
    },
    {
      "key": "registration.actions",
      "status": "unvalidated",
      "paperclip_surface_name": "ctx.actions.register action API",
      "requirement_ids": ["C7", "A5"],
      "downstream_consumers": ["approve-batch action", "Betting Table Approve Batch", "plugin-bos-light/src/worker.ts"],
      "evidence_source": "Draft manifest requests actions.register and worker.ts calls ctx.actions?.register; no host action invocation evidence exists.",
      "proof_command": "Future runtime action probe invoking approve-batch from Paperclip UI.",
      "runtime_evidence_field": "paperclip.registration.actions.registered_action_keys",
      "fallback_path": "Use a managed approval issue/comment workflow when host actions are unavailable.",
      "blocker_text": "Do not expose Approve Batch as a working native action until action registration and invocation are proven.",
      "manifest_capabilities": ["actions.register"],
      "adapter_assumptions": ["ctx.actions?.register"],
      "notes": "Action registration is not equivalent to native approval creation; approvals.native must also pass."
    },
    {
      "key": "config.api",
      "status": "fallback-only",
      "paperclip_surface_name": "Plugin config read/write API",
      "requirement_ids": ["C7", "A11a"],
      "downstream_consumers": ["BOS config", "company-template import defaults", "persistence restore strategy"],
      "evidence_source": "Draft manifest requests config.read/config.write, but no ctx.config call or runtime evidence exists yet.",
      "proof_command": "Future config read/write round-trip against Paperclip runtime.",
      "runtime_evidence_field": "paperclip.config.round_trip_ok",
      "fallback_path": "Restore BOS config from config JSON and company-template artifacts; do not store durable config only in plugin state.",
      "blocker_text": "Native plugin config cannot be treated as durable until read/write round-trip is proven.",
      "manifest_capabilities": ["config.read", "config.write"],
      "adapter_assumptions": [],
      "notes": "Docs/05 says config JSON and company template import files are the fallback."
    },
    {
      "key": "state.issue_scoped",
      "status": "unvalidated",
      "paperclip_surface_name": "Issue-scoped plugin state read/write API",
      "requirement_ids": ["C3", "C7", "A2", "A6", "A7", "A8", "A9", "A11b", "A11d", "A11e"],
      "downstream_consumers": ["BPI score overlay", "gate results", "circuit breaker state", "decision metadata", "plugin-bos-light/src/persistence.ts"],
      "evidence_source": "Persistence layer is in-memory only and docs require issue-scoped state spike before design freeze.",
      "proof_command": "Future issue-scoped ctx.state set/get round-trip with restart/readback evidence.",
      "runtime_evidence_field": "paperclip.state.issue_scoped.round_trip_ok",
      "fallback_path": "Mirror important results into issue documents/comments and reconstruct from native artifacts after state loss.",
      "blocker_text": "Do not make issue-scoped plugin state the only durable record until round-trip and restore evidence exists.",
      "manifest_capabilities": ["state.read", "state.write"],
      "adapter_assumptions": ["InMemoryBOSPersistence"],
      "notes": "Issue-scoped state is desired for fast UI overlays but must be recoverable from native artifacts."
    },
    {
      "key": "state.company_scoped",
      "status": "fallback-only",
      "paperclip_surface_name": "Company-scoped plugin state read/write API",
      "requirement_ids": ["C3", "C7", "A4", "A11a", "A11c"],
      "downstream_consumers": ["Betting Table current cycle", "BOS config", "company-level circuit/config cache"],
      "evidence_source": "docs/07_RISKS_AND_SPIKES.md records company-scoped ctx.state readback as a known risk.",
      "proof_command": "Future company-scoped ctx.state set/get/restart spike with readback evidence.",
      "runtime_evidence_field": "paperclip.state.company_scoped.round_trip_ok",
      "fallback_path": "Use config JSON, managed native issues/projects, and reconstructable artifacts instead of company-scoped state for durable truth.",
      "blocker_text": "Do not depend exclusively on company-scoped plugin state for Betting Table, circuit state, or config.",
      "manifest_capabilities": ["state.read", "state.write"],
      "adapter_assumptions": ["InMemoryBOSPersistence"],
      "notes": "This surface intentionally remains fallback-only until C3/C7 retire the known runtime risk."
    },
    {
      "key": "entities.api",
      "status": "fallback-only",
      "paperclip_surface_name": "Plugin entity read/write API",
      "requirement_ids": ["C7", "A4", "A11c"],
      "downstream_consumers": ["Betting Table cycle entities", "decision/circuit lookup indexes"],
      "evidence_source": "Draft manifest requests entities.read/entities.write, but no adapter implementation or runtime proof exists.",
      "proof_command": "Future entity create/read/update probe against Paperclip runtime.",
      "runtime_evidence_field": "paperclip.entities.round_trip_ok",
      "fallback_path": "Represent recoverable cycle state in managed native issue/project artifacts until entity APIs are confirmed.",
      "blocker_text": "Do not design Betting Table durability around plugin entities until read/write proof exists.",
      "manifest_capabilities": ["entities.read", "entities.write"],
      "adapter_assumptions": [],
      "notes": "Entities may become the right abstraction later, but current code does not prove them."
    },
    {
      "key": "activity.logging",
      "status": "unvalidated",
      "paperclip_surface_name": "Activity logging write API",
      "requirement_ids": ["C7", "A8", "A9", "A10"],
      "downstream_consumers": ["Circuit Breaker fallback scan", "health report diagnostics", "PaperclipAdapter.logActivity"],
      "evidence_source": "Draft manifest requests activity.write and adapter exposes logActivity, but in-memory adapter no-ops it.",
      "proof_command": "Future activity write/read visibility probe against Paperclip activity log.",
      "runtime_evidence_field": "paperclip.activity.write_visible",
      "fallback_path": "Attach issue comments or managed escalation issue updates when activity logging is unavailable.",
      "blocker_text": "Circuit Breaker fallback cannot depend on activity logs until write and scan behavior are proven.",
      "manifest_capabilities": ["activity.write"],
      "adapter_assumptions": ["logActivity"],
      "notes": "Activity logging is both an observability surface and a fallback signal source."
    },
    {
      "key": "events.issue_lifecycle",
      "status": "unvalidated",
      "paperclip_surface_name": "Issue lifecycle event subscription API",
      "requirement_ids": ["C7", "A2", "A6", "A7", "A8", "A9"],
      "downstream_consumers": ["BPI on issue.created", "gate transitions", "circuit breaker transitions"],
      "evidence_source": "Draft manifest requests events.subscribe, but no worker subscription is implemented or observed.",
      "proof_command": "Future event subscription probe that creates/updates an issue and records delivered event payloads.",
      "runtime_evidence_field": "paperclip.events.issue_lifecycle.delivered_event_types",
      "fallback_path": "Use explicit tool/action invocation and bounded polling where lifecycle events are missing.",
      "blocker_text": "Do not assume event-driven issue transitions until event delivery evidence exists.",
      "manifest_capabilities": ["events.subscribe"],
      "adapter_assumptions": [],
      "notes": "Issue events and terminal run events are split because they can have different delivery behavior."
    },
    {
      "key": "events.terminal_runs",
      "status": "fallback-only",
      "paperclip_surface_name": "Terminal/agent run event subscription API",
      "requirement_ids": ["C2", "C7", "A8", "A9", "A10"],
      "downstream_consumers": ["Circuit Breaker run-failure detection", "polling fallback"],
      "evidence_source": "docs/07_RISKS_AND_SPIKES.md records run events as declared-but-maybe-not-emitted in some runtime versions.",
      "proof_command": "Future event emission spike that triggers run finished/failed/cancelled and records plugin bus delivery.",
      "runtime_evidence_field": "paperclip.events.terminal_runs.delivered_event_types",
      "fallback_path": "Poll active runs only with jitter/backoff and fall back to activity-log scanning.",
      "blocker_text": "Circuit Breaker must not rely only on run events until C2/C7 prove emission in the target runtime.",
      "manifest_capabilities": ["events.subscribe"],
      "adapter_assumptions": [],
      "notes": "Known fallback posture: poll_scope=ACTIVE_RUNS_ONLY, interval_ms=30000, jitter_ms=5000, backoff_after_attempts=10."
    },
    {
      "key": "issues.native",
      "status": "unvalidated",
      "paperclip_surface_name": "Native issue read/write/create API",
      "requirement_ids": ["C7", "A9", "A11c"],
      "downstream_consumers": ["escalation issue creation", "managed Betting Table issue/project", "PaperclipAdapter.createEscalationIssue"],
      "evidence_source": "Draft manifest requests issues.read/issues.write and adapter exposes createEscalationIssue, but implementation is in-memory only.",
      "proof_command": "Future native issue create/read/update probe against Paperclip runtime.",
      "runtime_evidence_field": "paperclip.issues.create_read_update_ok",
      "fallback_path": "If native issue APIs are absent, write escalation details to comments/documents and require manual issue creation.",
      "blocker_text": "Do not claim automated escalation issue creation until native issues API proof exists.",
      "manifest_capabilities": ["issues.read", "issues.write"],
      "adapter_assumptions": ["createEscalationIssue"],
      "notes": "Native issue access is required for Paperclip-visible circuit breaker escalation."
    },
    {
      "key": "documents.native",
      "status": "unvalidated",
      "paperclip_surface_name": "Native issue document API",
      "requirement_ids": ["C7", "A3", "A6", "A11b", "A11d"],
      "downstream_consumers": ["Blueprint generation", "gate result persistence", "BPI/gate restore", "PaperclipAdapter.createIssueDocument"],
      "evidence_source": "Adapter exposes createIssueDocument but current implementation stores documents in memory only; manifest has no explicit documents capability.",
      "proof_command": "Future issue document create/read probe against Paperclip runtime.",
      "runtime_evidence_field": "paperclip.documents.create_read_ok",
      "fallback_path": "Use issue description or comments for markdown artifacts if native documents are unavailable.",
      "blocker_text": "Blueprint and gate documents must not be described as native Paperclip documents until create/read proof exists.",
      "manifest_capabilities": [],
      "adapter_assumptions": ["createIssueDocument"],
      "notes": "Documents are an adapter-assumed surface rather than a manifest-requested one today."
    },
    {
      "key": "comments.native",
      "status": "unvalidated",
      "paperclip_surface_name": "Native issue comment API",
      "requirement_ids": ["C7", "A6", "A7", "A11d", "A11e"],
      "downstream_consumers": ["gate guidance", "decision records", "restore from issue history", "PaperclipAdapter.addIssueComment"],
      "evidence_source": "Adapter exposes addIssueComment but current implementation stores comments in memory only; manifest has no explicit comments capability.",
      "proof_command": "Future issue comment create/read probe against Paperclip runtime.",
      "runtime_evidence_field": "paperclip.comments.create_read_ok",
      "fallback_path": "Use issue documents or issue descriptions where native comments are unavailable.",
      "blocker_text": "Do not rely on issue history restore from comments until native comment behavior is proven.",
      "manifest_capabilities": [],
      "adapter_assumptions": ["addIssueComment"],
      "notes": "Comments are the preferred human-visible fallback for several persistence paths if supported."
    },
    {
      "key": "approvals.native",
      "status": "unvalidated",
      "paperclip_surface_name": "Native approval/request creation API",
      "requirement_ids": ["C7", "A5"],
      "downstream_consumers": ["approve-batch action", "Betting Table batch approval", "PaperclipAdapter.createApprovalRequest"],
      "evidence_source": "worker.ts calls ctx.approvals?.create and adapter exposes createApprovalRequest, but no Paperclip runtime approval was created.",
      "proof_command": "Future approve-batch runtime probe that creates and reads a native Paperclip approval/request.",
      "runtime_evidence_field": "paperclip.approvals.create_read_ok",
      "fallback_path": "Create a manual approval issue/comment requesting review; never substitute a plugin-side decision for native approval.",
      "blocker_text": "A5 cannot pass until Paperclip-native approval/request creation is proven.",
      "manifest_capabilities": [],
      "adapter_assumptions": ["ctx.approvals?.create", "createApprovalRequest"],
      "notes": "The fallback can request human approval but must not pretend to be a native approval object."
    },
    {
      "key": "ui.dashboard_widgets",
      "status": "unvalidated",
      "paperclip_surface_name": "Dashboard widget UI slot",
      "requirement_ids": ["C7", "A4"],
      "downstream_consumers": ["betting-table dashboard widget", "Pitch Deck rendering"],
      "evidence_source": "Draft manifest declares dashboard_widgets=['betting-table']; no runtime UI slot rendering evidence exists.",
      "proof_command": "Future UI smoke test that loads the Paperclip dashboard and verifies betting-table slot render.",
      "runtime_evidence_field": "paperclip.ui.dashboard_widgets.rendered_ids",
      "fallback_path": "Publish Betting Table as a managed native issue/project or markdown artifact when dashboard widgets are unavailable.",
      "blocker_text": "Do not promise dashboard Pitch Deck rendering until Paperclip renders the declared widget.",
      "manifest_capabilities": [],
      "manifest_ui": {"dashboard_widgets": ["betting-table"], "issue_detail_tabs": []},
      "adapter_assumptions": [],
      "notes": "UI slot support depends on both manifest schema acceptance and runtime rendering."
    },
    {
      "key": "ui.issue_detail_tabs",
      "status": "unvalidated",
      "paperclip_surface_name": "Issue detail tab UI slots",
      "requirement_ids": ["C7", "A2", "A6", "A8", "A9"],
      "downstream_consumers": ["bos-status tab", "circuit-state tab", "gate-results tab"],
      "evidence_source": "Draft manifest declares issue_detail_tabs but no Paperclip issue view has rendered them.",
      "proof_command": "Future UI smoke test that opens a Paperclip issue and verifies bos-status, circuit-state, and gate-results tabs.",
      "runtime_evidence_field": "paperclip.ui.issue_detail_tabs.rendered_ids",
      "fallback_path": "Surface status, circuit, and gate results through issue labels/comments/documents when tabs are unavailable.",
      "blocker_text": "Do not present issue overlay tabs as available until runtime UI rendering evidence exists.",
      "manifest_capabilities": [],
      "manifest_ui": {"dashboard_widgets": [], "issue_detail_tabs": ["bos-status", "circuit-state", "gate-results"]},
      "adapter_assumptions": [],
      "notes": "The tab IDs are mapped from the draft manifest to catch drift."
    }
  ]
}
